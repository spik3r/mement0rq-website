import styled from "styled-components";

export const Badge = styled.code`
  font: 500 10px/1 'JetBrains Mono', ui-monospace, monospace;
  color: var(--muted);
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: 4px;
  padding: 3px 6px;
  text-transform: uppercase;
  letter-spacing: 0.08em;
`;
